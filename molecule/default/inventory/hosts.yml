---
all:
  children:
    molecule:
      hosts:
        molecule-ubuntu:
          ansible_connection: community.docker.docker
          container_image: ubuntu:latest

        molecule-oraclelinux8:
          ansible_connection: community.docker.docker
          container_image: oraclelinux:8
          ansible_python_interpreter: /usr/bin/python3.11